import FormatDate from "./formatDate"
import IsWorkday from "./isWorkday"

const time = {
    formatDate: FormatDate,
    isWorkday: IsWorkday
}

export const {
    formatDate,
    isWorkday
} = time

export default time
